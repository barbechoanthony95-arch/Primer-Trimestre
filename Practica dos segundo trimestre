void loop() {
  thing.handle();
} #include <ThingerESP32.h>
#include <WiFi.h>
#include <PubSubClient.h>
#include <DHT.h>
#include <Adafruit_Sensor.h>

// ---------- WIFI ----------
const char* ssid = "iPhone de Anthony";
const char* password = "matuteP12345";

// ---------- THINGER.IO ----------
#define USERNAME "anthony"
#define DEVICE_ID "Humedad_esp"
#define DEVICE_CREDENTIAL "123456789"
const char* mqtt_server = "backend.thinger.io";

// ---------- DHT ----------
#define DHTPIN 23
#define DHTTYPE DHT11
DHT dht(DHTPIN, DHTTYPE);

// ---------- MQTT ----------
WiFiClient espClient;
PubSubClient client(espClient);

// ---------- TOPIC ----------
String topic = "v1/users/" USERNAME "/devices/" DEVICE_ID "/data/DHT11";

// ---------- CONECTAR WIFI ----------
void setup_wifi() {
  delay(10);
  Serial.println("Conectando a WiFi...");
  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.print(".");
  }
