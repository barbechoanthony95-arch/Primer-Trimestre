importar red
desde máquina importar Pin
importar espnow
importar utime

Para enviar/recibir datos, debe estar activa una interfaz WLAN.
`sta = network.WLAN(network.STA_IF)` # O `network.AP_IF` `
sta.active(True)`
`sta.disconnect()` # Para ESP8266

Inicializar ESP-NOW
esp = espnow.ESPNow()
esp.active(True)

Define la dirección MAC del ESP32 receptor (ESP32 B)
peer = b'x!\x84\xc68\xb0'
esp.add_peer(peer)

Crea una función para enviar datos cuando se pulse un botón (opcional)
button_pin = Pin(23, Pin.IN, Pin.PULL_UP)

Inicializar variables para el debounce
last_button_state = 1 # Suponiendo que el botón no está presionado inicialmente
debounce_delay = 50 # Ajusta este valor según tus necesidades (milisegundos)

mientras que es verdadero:
Lee el estado actual del botón
estado_actual_botón = pin_botón.valor()

Comprueba si el estado del botón ha cambiado.
if current_button_state != last_button_state:
# Esperar un breve tiempo para eliminar el rebote del botón
utime.sleep_ms(debounce_delay)

# Read the button state again to make sure it's stable
current_button_state = button_pin.value()

# If the button state is still different, it's a valid press
if current_button_state != last_button_state:
    if current_button_state == 0:
        message = "ledOn"
        print(f"Sending command : {message}")
        esp.send(peer, message)
    else:
        message = "ledOff"
        print(f"Sending command : {message}")
        esp.send(peer, message)

# Update the last button state
last_button_state = current_button_state
